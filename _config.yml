title: SUNAVE-DEVLOG
description: Writing down before forgetting
theme: just-the-docs
baseurl: "" # the subpath of your site, e.g. /blog
url: https://choisohan.github.io/sunave-devlog

aux_links: # remove if you don't want this link to appear on your pages
  Template Repository: https://github.com/choisohan/sunave-devlog

permalink: pretty

defaults:
  - scope:
      path: "docs" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "default"
